[package]
name = "registry"
version = "0.1.0"
edition = "2021"
rust-version = "1.87.0" # Keep this in sync with the requirements!

[dependencies]
wasm-interpreter.workspace = true
checked = { workspace = true, features = ["interop"] }
