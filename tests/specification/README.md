# Testsuite Runner

## Using the Testsuite Runner

1. `git submodule update --init` to fetch the testsuite submodule
1. Use environment variables as documented in `mod.rs` to configure the
   testsuite runner
