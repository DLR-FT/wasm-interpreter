//! TODO
//!
//! Reimplement a small registry (like in [`registry`]) to show how a similar API to other Wasm
//! runtimes is easily achievable.

use std::error::Error;

fn main() -> Result<(), Box<dyn Error>> {
    todo!();
}
